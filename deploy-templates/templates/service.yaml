apiVersion: v1
kind: Service
metadata:
  name: {{ include "worldclock.fullname" . }}
  labels:
    {{- include "worldclock.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "worldclock.selectorLabels" . | nindent 4 }}
